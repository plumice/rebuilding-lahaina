---
interface Props {
  src: string;
  alt: string;
  title: string;
  variant?: 'board' | 'full';
}

const { src, alt, title, variant = 'full' } = Astro.props;
---

<figure class:list={[
  'drawing-figure',
  variant === 'board'
    ? 'mx-auto max-w-[56rem] border border-[#e8e4dc] bg-[#f9f7f3] p-4 sm:p-6'
    : 'border border-[#ececea] bg-[#fafaf9] p-4 sm:p-6',
]}>
  <img
    src={src}
    alt={alt}
    class:list={[
      'zoomable cursor-zoom-in',
      variant === 'board'
        ? 'mx-auto block h-auto max-h-[78vh] w-auto max-w-full object-contain'
        : 'mx-auto block h-auto max-h-[85vh] w-auto max-w-full object-contain',
    ]}
    loading="lazy"
    tabindex="0"
    role="button"
    aria-label={`Press Enter to enlarge: ${title}`}
  />
</figure>

<style>
  .drawing-figure {
    margin: 0;
  }
</style>

<script>
  import mediumZoom from 'medium-zoom';
  const zoom = mediumZoom('.zoomable', {
    background: 'rgba(255, 255, 255, 0.95)',
    margin: 24,
  });
  document.querySelectorAll<HTMLImageElement>('.zoomable').forEach((img) => {
    img.addEventListener('keydown', (e: KeyboardEvent) => {
      if (e.key === 'Enter' || e.key === ' ') {
        e.preventDefault();
        zoom.open({ target: img });
      }
    });
  });
</script>
