---
const scales = [
  { id: 'regional', label: 'West Maui' },
  { id: 'district', label: 'District' },
  { id: 'town', label: 'Lahaina Town' },
  { id: 'site', label: 'Neighborhoods' },
  { id: 'node', label: 'Buildings' },
];

interface Props {
  active?: string;
}

const { active } = Astro.props;
---

<nav class="flex gap-1 mb-8" aria-label="Scale navigation">
  {scales.map((scale) => (
    <a
      href={`/scale/${scale.id}`}
      class:list={[
        'px-4 py-3 text-[0.8rem] tracking-widest uppercase no-underline transition-colors border',
        active === scale.id
          ? 'bg-accent text-bg border-accent'
          : 'bg-surface text-text-muted border-border hover:text-text hover:border-accent',
      ]}
    >
      {scale.label}
    </a>
  ))}
</nav>
